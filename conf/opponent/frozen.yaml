kind: frozen
spec: frozen                    # eval CLI reads cfg.opponent.model_path separately
_target_: envs.quidditch.opponents.FrozenPolicyOpponent
_partial_: true
model_path: ???                     # experiment must override
deterministic: false
