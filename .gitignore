# Python
__pycache__
*.pyc

# Dev/training artifacts (ephemeral — use `make promote` to save good models)
runs/

# models/ IS tracked; individual zips committed via `make promote`

# Working configs — gitignored so local experiments aren't committed.
# Edit freely; `make promote` snapshots training/env into models/<run>/.
# Restore from a promoted model with `make repro MODEL=<name>`.
# First-time setup: `make install` copies templates/*.toml here.
config/