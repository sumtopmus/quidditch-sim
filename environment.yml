name: uav
# MuJoCo is installed via pip (official wheels support macOS Apple Silicon).
# No conda-forge physics library needed — pybullet/PyFlyt are gone.
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy
  - wheel
  - pip
  - pip:
    - -r requirements.txt
